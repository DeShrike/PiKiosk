sudo apt-get update
sudo apt-get install python3-pip

pip3 install websocket-client
pip3 install requests

pip3 install flask

sudo apt-get install samba samba-common-bin

sudo nano /etc/samba/smb.conf

git clone

[pi]
   path = /home/pi
   writeable = yes
   create mask = 0666
   directory mask = 0755
   public = no
   browseable = yes

[kioskimages]
   path = /home/pi/pikiosk/app/static/images
   writeable = yes
   create mask = 0666
   directory mask = 0755
   public = no
   browseable = yes

sudo smbpasswd -a pi

sudo systemctl restart smbd


sudo iptables -t nat -A PREROUTING -p tcp --dport 80 -j REDIRECT --to-port 3001

